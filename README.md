# ⚡️ VC Blog (Neo-Brutalism Edition)

Современный, дерзкий и высокопроизводительный движок блога, построенный на стеке **Next.js 15**, **TypeScript** и **Prisma**. Проект выполнен в стиле **необрутализма** с акцентом на высокую контрастность, жесткие формы и отличную читаемость.

## 🚀 Технологический стек

- **Framework**: [Next.js 15](https://nextjs.org/) (App Router, Server Actions)
- **Language**: [TypeScript](https://www.typescript.org/)
- **Database**: [SQLite](https://www.sqlite.org/) (локально через Prisma)
- **ORM**: [Prisma](https://www.prisma.io/)
- **Styling**: [Tailwind CSS](https://tailwindcss.com/)
- **UI/UX**: Custom Neo-Brutalism Design (High contrast, thick borders, hard shadows)
- **Editor**: [TipTap](https://tiptap.dev/) (Headless Rich Text Editor)
- **Images**: [Sharp](https://sharp.pixelplumbing.com/) (обработка и ресайз изображений)
- **Auth**: [Jose](https://github.com/panva/jose) (JWT-based сессии в HttpOnly Cookies)

## ✨ Ключевые особенности

### 📝 Контент и Редактор
- **TipTap Editor**: Полноценный визуальный редактор с поддержкой форматирования (B, I, H2, H3, списки).
- **Медиа-система**: Загрузка обложек через Drag-n-Drop. Автоматический ресайз в 3 версии (`low`, `middle`, `original`) для оптимизации трафика.
- **Публикация**: Система черновиков. Статьи видны пользователям только после активации флага "Опубликовано".

### 🔒 Админ-панель (`/adm`)
- **Защищенный вход**: Авторизация по Email/Password.
- **Управление постами**: Полный CRUD (создание, чтение, обновление, удаление).
- **Модерация комментариев**: Просмотр всех комментариев, возможность скрытия или удаления.
- **Настройки**: Смена пароля администратора.
- **Автоматизация**: Физическое удаление связанных изображений при удалении поста.

### 🎨 Дизайн и UX
- **Neo-Brutalism**: Уникальный визуальный стиль с жирными границами, яркими цветами и жесткими тенями.
- **Dark Mode**: Полноценная поддержка темной темы (адаптированный необрутализм).
- **Responsive**: Полная адаптивность под мобильные устройства.
- **iOS-like Interactions**: Сохранение приятных микро-взаимодействий (нажатия, переходы).

## 🛠 Установка и развертывание

### 1. Клонирование
```bash
git clone <url-вашего-репозитория>
cd vc_blog_ts_react
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Настройка базы данных
```bash
# Создание базы данных и применение схемы
npx prisma db push

# Наполнение начальными данными (сид)
npx prisma db seed
```

### 4. Запуск в режиме разработки
```bash
npm run dev
```
Откройте [http://localhost:3000](http://localhost:3000) для просмотра блога.
Админка доступна по адресу [http://localhost:3000/adm](http://localhost:3000/adm).

**Реквизиты входа по умолчанию:**
- **Email**: `admin@mail.com`
- **Password**: `password`

## 📦 Деплой

Проект оптимизирован для деплоя на [Vercel](https://vercel.com/). 

**Важно для SQLite**: 
SQLite — это локальный файл. При деплое на Vercel (который является serverless), изменения в БД будут сбрасываться при каждом перезапуске функций. 
- Для **Production** рекомендуется переключить `datasource` в `schema.prisma` на PostgreSQL или MySQL (например, через Supabase или Neon).
- Для быстрого тестирования можно использовать `db push` на этапе сборки.

## 📜 Лицензия
MIT
