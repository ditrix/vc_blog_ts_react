# ⚡️ VC Blog (Neo-Brutalism Edition)

Сучасний, сміливий та високопродуктивний двигун блогу, побудований на стеку **Next.js 15**, **TypeScript** та **Prisma**. Проєкт виконаний у стилі **необруталізму** з акцентом на високу контрастність, жорсткі форми та відмінну читабельність.

> **Примітка**: Цей проєкт створений розробником, чий основний стек технологій не включає TypeScript та React. Проєкт був розроблений з метою вивчення та практики **вайбкодінгу** (Vibe-Coding). Це робота досвідченого спеціаліста, який не є фронтенд-фулстеком у повсякденній практиці, але використовує сучасні інструменти для швидкого прототипування та навчання.

## 🚀 Технологічний стек

- **Framework**: [Next.js 15](https://nextjs.org/) (App Router, Server Actions)
- **Language**: [TypeScript](https://www.typescript.org/)
- **Database**: [SQLite](https://www.sqlite.org/) (локально через Prisma)
- **ORM**: [Prisma](https://www.prisma.io/)
- **Styling**: [Tailwind CSS](https://tailwindcss.com/)
- **UI/UX**: Custom Neo-Brutalism Design (High contrast, thick borders, hard shadows)
- **Editor**: [TipTap](https://tiptap.dev/) (Headless Rich Text Editor)
- **Images**: [Sharp](https://sharp.pixelplumbing.com/) (обробка та ресайз зображень)
- **Auth**: [Jose](https://github.com/panva/jose) (JWT-based сесії в HttpOnly Cookies)

## ✨ Ключові особливості

### 📝 Контент та Редактор
- **TipTap Editor**: Повноцінний візуальний редактор з підтримкою форматування (B, I, H2, H3, списки).
- **Медіа-система**: Завантаження обкладинок через Drag-n-Drop. Автоматичний ресайз у 3 версії (`low`, `middle`, `original`) для оптимізації трафіку.
- **Публікація**: Система чернеток. Статті видно користувачам тільки після активації прапорця "Опубліковано".

### 🔒 Адмін-панель (`/adm`)
- **Захищений вхід**: Авторизація за Email/Password.
- **Управління постами**: Повний CRUD (створення, читання, оновлення, видалення).
- **Модерація коментарів**: Перегляд усіх коментарів, можливість приховати або видалити.
- **Налаштування**: Зміна пароля адміністратора.
- **Автоматизація**: Фізичне видалення пов'язаних зображень при видаленні поста.

### 🎨 Дизайн та UX
- **Neo-Brutalism**: Унікальний візуальний стиль із жирними межами, яскравими кольорами та жорсткими тінями.
- **Dark Mode**: Повноцінна підтримка темної теми (адаптований необруталізм).
- **Responsive**: Повна адаптивність під мобільні пристрої.
- **Interactive**: Ефекти натискання (neo-press) для всіх активних елементів.

## 🛠 Встановлення та розгортання

### 1. Клонування
```bash
git clone <url-вашого-репозиторію>
cd vc_blog_ts_react
```

### 2. Встановлення залежностей
```bash
npm install
```

### 3. Налаштування бази даних
```bash
# Створення бази даних та застосування схеми
npx prisma db push

# Наповнення початковими даними (seed)
npx prisma db seed
```

### 4. Запуск у режимі розробки
```bash
npm run dev
```
Відкрийте [http://localhost:3000](http://localhost:3000) для перегляду блогу.
Адмінка доступна за адресою [http://localhost:3000/adm](http://localhost:3000/adm).

**Реквізити входу за замовчуванням:**
- **Email**: `admin@mail.com`
- **Password**: `password`

## 📦 Деплой

Проєкт оптимізований для деплою на [Vercel](https://vercel.com/). 

**Важливо для SQLite**: 
SQLite — це локальний файл. При деплої на Vercel (який є serverless), зміни в БД будуть скидатися при кожному перезапуску функцій. 
- Для **Production** рекомендується переключити `datasource` в `schema.prisma` на PostgreSQL або MySQL.

## 📜 Журнал розробки
Детальна історія змін та прийнятих рішень доступна у файлі [JOURNAL.md](./.cursor/docs/JOURNAL.md).

## 📜 Ліцензія
MIT
