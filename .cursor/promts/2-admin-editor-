---
description: Расширение функционала админ-панели: текстовый редактор и загрузка изображений
globs: "app/adm/**/*, app/api/**/*, lib/actions.ts"
---

# ✍️ Админка: Редактор и Медиа (Дополнение)

## 📋 ОБЗОР ЗАДАЧИ
Необходимо расширить функционал создания/редактирования постов в разделе `/adm`, внедрив современный текстовый редактор и систему загрузки обложек для статей.

## 🛠 ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ

### 1. Текстовый Редактор
- **Решение**: Использовать бесплатный и кастомизируемый редактор (рекомендуется **TipTap** или **React Quill**).
- **Функционал**:
    - Базовое форматирование: жирный, курсив, заголовки (H2, H3).
    - Списки (маркированные и нумерованные).
    - Очистка от лишних HTML-тегов при вставке.
- **Интеграция**: Редактор должен быть интегрирован в существующую форму Server Actions.

### 2. Загрузка Изображений
- **Интерфейс**: 
    - Поле загрузки с поддержкой **Drag-n-Drop**.
    - Выбор файла с диска.
    - Визуальный индикатор (превью) после выбора.
- **Хранение**: Локальная загрузка в `public/uploads/` (или интеграция с вашим текущим решением).
- **Логика БД**: Обновить модель `Post` в Prisma, добавив поле `imagePath` (String, optional).

### 3. Отображение (UI/UX)
- **iOS Style**: Все элементы управления должны соответствовать гайдлайнам iOS (закругленные углы, мягкие тени, стеклянные эффекты).
- **Адаптивность**:
    - **Preview**: Если изображение приаттачено, оно отображается в ленте постов (миниатюра `lowsize`).
    - **Detail View**: В полной статье отображается средний размер (`middle size`).
    - **Empty State**: Если изображения нет, блок под него не резервируется и не отображается.

## 🏗 КОНТЕКСТ ПРИМЕНЕНИЯ
- **Маршруты**: `/adm/post/[id]` и `/adm/post/new`.
- **Стек**: Next.js 15, Tailwind CSS, Prisma, Server Actions.
- **Безопасность**: Загрузка доступна только авторизованным пользователям через Middleware.
